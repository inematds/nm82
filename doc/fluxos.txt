Perfeito, Nei 👌 — aqui vai o **resumo direto e prático dos fluxos principais** do seu sistema Supabase + n8n:

---

## ⚙️ **1. Fluxo de E-mails**

* Gmail envia → `emails`
* Anexos vão → `email_attachments`
* Se for comprovante → cria ou atualiza `pagamentos`
* O pagamento atualiza `pessoas_fisicas`
  → valor, data e tipo de assinatura

---

## 💰 **2. Fluxo Financeiro**

* Cada `pagamento` tem `pessoa_id` (ligado a `pessoas_fisicas.id`)
* Atualiza automaticamente:

  * `valor_ultimo_pagamento`
  * `data_ultimo_pagamento`
  * `tipo_assinatura` (≥100 = anual / <100 = mensal)

---

## 🤝 **3. Fluxo de Convites**

* `pessoas_fisicas` pode gerar `codigos_convite`
* Cada código define plano, validade e usos
* Quando alguém usa o código:

  * é registrado em `afiliados`
  * `convites_usados` e `usos_atuais` são incrementados
  * novo afiliado é inserido em `pessoas_fisicas`

---

## 👤 **4. Estrutura Central**

```
pessoas_fisicas
 ├─ pagamentos (histórico financeiro)
 ├─ codigos_convite (gerados pelo membro)
 └─ afiliados (quem entrou via convite)
```

---

## 📈 **5. Resultado Final**

* **emails** → capturam dados e comprovantes
* **pagamentos** → registram transações
* **pessoas_fisicas** → consolidam informações pessoais e financeiras
* **codigos_convite + afiliados** → gerenciam o crescimento da comunidade

---